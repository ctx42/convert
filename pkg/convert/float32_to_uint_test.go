// SPDX-FileCopyrightText: (c) 2026 Rafal Zajac
// SPDX-License-Identifier: MIT

// Code generated by 00_generate_main.go. DO NOT EDIT.

package convert

import (
	"github.com/ctx42/testing/pkg/assert"
	"math"
	"testing"
)

func Test_Float32ToUint_tabular(t *testing.T) {
	tt := []struct {
		testN string

		value float32
		want  uint
		err   error
		msg   string
	}{
		{
			"error - must be a number",
			float32(math.NaN()),
			0,
			ErrInvValue,
			"invalid value: from float32 to uint",
		},
		{
			"error - negative infinity",
			float32(math.Inf(-1)),
			0,
			ErrInvValue,
			"invalid value: from float32 to uint",
		},
		{
			"error - positive infinity",
			float32(math.Inf(1)),
			0,
			ErrInvValue,
			"invalid value: from float32 to uint",
		},
		{
			"error - fraction",
			4.2,
			0,
			ErrFraction,
			"must be a whole number: from float32 to uint",
		},
		{
			"error - negative",
			-1,
			0,
			ErrInvRange,
			"value out of range: from float32 to uint",
		},
		{
			"error - safe overflow",
			Float32SafeIntMax + 1,
			0,
			ErrInvSafeRange,
			"value out of safe range: from float32 to uint",
		},
		{"success", 42, 42, nil, ""},
	}

	for _, tc := range tt {
		t.Run(tc.testN, func(t *testing.T) {
			// --- When ---
			have, err := Float32ToUint(tc.value)

			// --- Then ---
			if tc.err == nil {
				assert.NoError(t, err)
				assert.Equal(t, tc.want, have)
				assert.Equal(t, tc.value, float32(have))
				return
			}

			assert.ErrorIs(t, tc.err, err)
			assert.ErrorEqual(t, tc.msg, err)
			assert.Equal(t, uint(0), have)
		})
	}
}
